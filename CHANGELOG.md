# 修改说明 / Changelog

本项目基于上游 [shy3130/tick-stock-panel](https://github.com/shy3130/tick-stock-panel) 的分支定制。

**版本规则**：分支版本号 = 上游版本号 + `-修订序号`，修订序号与上游版本**剥离、独立累计**（每次更新 +0.01）。
- 例：上游 0.1.88 → 首次修订 `0.1.88-0.01`，第二次 `0.1.88-0.02`；
- 上游升级到 0.1.89 后**序号不重置**，继续 `0.1.89-0.03`；
- 序号即「我对项目做过的修订次数」，与上游版本无关，方便追溯每次更新内容。

## 0.1.88-0.31 (2026-09-09)

### 日收益头部重构

- 年份切换居中置顶（2026 年 ◀ ▶），今日盈亏同行右侧
- 月份导航 ◀ 9 月 ▶ 居中显示在年份下方，当月盈亏紧随其后

## 0.1.88-0.30 (2026-09-09)

### 日收益日历头部去重

- 日历头部去掉「2026 年」年份（与外层年份切换器重复），仅显示「9 月」
- 左右切换箭头贴住月份（◀ 9月 ▶），样式加大加粗
- 当月盈亏移至右端独立展示

## 0.1.88-0.29 (2026-09-09)

### 持仓收益区布局微调

- 日收益卡与左列（持仓结构+盈亏贡献）等高，日历网格随高度缩放填满
- 月收益、年收益为独立全宽卡，上下排列、同宽

## 0.1.88-0.28 (2026-09-09)

### 持仓全站标记 + ETF 修复 + B/S + 布局重构

- **全站持仓标记**：新增 `held-symbols` 端点（全部账户持有即标），选股/策略结果表的 symbol 处显示「持」徽章（自选/详情已有）
- **修复 ETF 无价格**：持仓内 CN ETF 走独立存储取行情（价值ETF富国 512040 → 1.225）
- **B/S 买卖点**：持仓页点开个股详情，K 线图上以 B/S 箭头标注买卖点（label 含价格，如 B 913.5），从每日持仓快照变动推导
- **收益区布局重构**：日/月/年从分页拆出上下排列；持仓结构与个股盈亏贡献上下排列放左列，日收益在其右侧对齐
- 自选列表持仓标记改为任一账户持有即标

## 0.1.88-0.27 (2026-09-09)

### 收尾优化

- **收益日历账本权威值**：月/年收益有账本缓存时优先展示并标注「账本」（数值来源明确）
- **明细表布局回归**：列菜单溢出修复（标题行 relative 展开）、表格 min-width 自适应横向滚动、不裁剪下拉
- **加载/错误状态推广**：共享 LoadingSkeleton/ErrorBanner 组件抽出；自选页行情加载失败显示重试横幅
- **修复港股价格刷新线程未启动**：main 漏调 `start_hk_price_refresh`（持仓港股开市时段 60s 自动刷新此前未生效），现已注册
- 新建 `services/background_tasks.py` 统一后台任务登记入口

## 0.1.88-0.26 (2026-09-09)

### 全项目数据加载优化

- **统一行情缓存层** `services/quote_cache.py`（15s TTL）：自选/持仓/个股弹窗共用，避免同 symbol 重复请求 TickFlow
- **自选页港美股去同步阻塞**：enriched 走行情缓存，命中毫秒级返回；未命中一次补齐后缓存
- **持仓市场字段冷启动异步化**：`_market_rows` 冷缓存不阻塞首屏（先返回本地落库价秒出，后台线程补齐 TickFlow 数据）；行情源失败仍显示上次数据
- **缓存一致性**：持仓增删改/导入后主动失效行情缓存
- **前端轮询收敛**：Dashboard/Monitor/Layout 移除后台无谓轮询（仅前台可见时刷新）
- 实测：自选 15ms（缓存命中）、持仓 6ms、看板 118ms

## 0.1.88-0.25 (2026-09-09)

### 页签当日盈亏修复 + 清仓分区完善

- **修复页签 -100.00% 错误**：账户列表端点未接入市场数据 → 现价缺失被当 0 计算。现接入 `_market_rows`（60s 缓存），页签当日盈亏金额与百分比正确
- **已清仓股票分区**：账本中数量为 0 的持仓（如 MINIMAX-W、长鑫科技）自动转为「已清仓」状态，归入「已清仓明细」折叠区（含成本/清仓时间），不再与持仓混排
- 汇总卡删除「近30日」迷你图（按反馈）

## 0.1.88-0.24 (2026-09-09)

### 资产趋势对接 + 交易日历 + 列自定义 + CSV 导出

- **真实资产曲线**：对接账本 `asset_trend` 接口（含每日出入金），年收益曲线使用账本真实历史资产（含出金记录，本金/出金自动核算）。
- **交易日历**：调度与同步用账本 `last_trading_day` 接口判断——节假日自动跳过同步（接口不可用时回退周一至五规则）。
- **明细表列自定义**：表头「列显示」勾选菜单，列显示配置持久化（localStorage）。
- **CSV 导出**：持仓明细 CSV（含全部增强字段）+ 日收益 CSV，UTF-8 BOM Excel 兼容。

## 0.1.88-0.23 (2026-09-09)

### 数据核查与七项 UI 修复

- **数据核查通过**：107W0754 账户 智谱 1000 股/成本 HK$999.549、中远海能 2300/21.531、MINIMAX-W、长鑫科技全部与券商一致；全部价格/涨跌幅/月度涨跌来自 TickFlow 本地数据
- **修复日历格子无颜色（根因）**：盈亏色值被当作 className 使用（无效）→ 改为内联 style，颜色填充真正生效
- **汇总卡布局统一**：固定双列（总资产左、累计盈亏右），不同账户/宽度不再漂移；总资产按累计盈亏正负配色
- **仓位进度条移到汇总卡**「持仓市值/仓位」处（进度条+百分比）；明细表占比恢复纯百分比
- **空默认账户页签自动隐藏**（有持仓时正常显示）
- **60s 轻量轮询**：持仓/汇总自动更新（本地数据读取，无外部请求压力）

## 0.1.88-0.22 (2026-09-09)

### 市场映射与数据修复
- **市场代码映射严格化**：6 位数字代码强制归沪深京，HK 仅接受 5 位码——修复中远海能 600026 被错误标「港」
- **价格解析兼容货币前缀**（HK$/$/¥）——修复 MINIMAX-W 等显示「—」
- **同步清理失效持仓行**：每账户同步后删除账本中已不存在的旧持仓行

### 持仓数据全面 TickFlow 化
- 新增 `_market_rows`：现价/涨跌幅/近1/3/6/12月涨跌全部从 **TickFlow 本地历史数据**计算（60s 进程内缓存），列表/汇总/分析统一数据源

### 新功能
- **清仓明细**：持仓明细下方折叠区，显示已清仓股票与已实现盈亏
- **现价涨跌配色**、**占比进度条**
- **B/S 买卖点**：`/api/holdings/{symbol}/trades` 从每日快照变动推导买卖事件，详情页 K 线标注
- **历史收益缓存**：对接账本日/月/年收益接口，设置弹窗显示缓存状态/一键获取（实测 21 个月数据与账本一致）

### 修复
- 汇总接口 500（本地化重构漏掉 name 字段映射）
- NaN 金额 JSON 序列化问题

## 0.1.88-0.21 (2026-09-09)

### 持仓本地优先重构（修慢 + 数据消失）

- **根因**：持仓列表/汇总接口每次请求同步等待 TickFlow 实时行情（单批 8s 超时+限流），行情失败时现价/涨跌幅/市值/浮动盈亏/当日盈亏全为 null，前端查询失败清空数据。
- **本地优先**：接口改为纯本地读取（落库的价格/涨跌幅/账本字段，毫秒响应，零外部请求）；行情价格仅在手动「同步」与定时（交易日 9:31/16:05）时更新并落库。
- **同步落库涨跌幅**（change_pct），当日盈亏直接用账本的当日参考盈亏。
- **加载提示**：汇总卡/明细表骨架屏；查询失败显示错误 + 重试按钮；placeholderData 保证刷新时旧数据不消失。
- 实测：接口响应 22ms（原 2-8 秒）。

## 0.1.88-0.20 (2026-09-09)

### 四项修复

1. **107W0754 账户加载不出/错误（根因修复）**：每次同步都按时间戳新建账户 id（未按名称复用），反复同步/重置后产生 12 个孤儿目录、有效账户 id 失效。修复：同步按名称复用账户 id（稳定不变）+ 清理孤儿目录 + 已重新同步（3 账户 8 持仓）。
2. **账户页签经常消失**：查询刷新瞬间数据被清空 → 加 `placeholderData`（保留上一次数据）+ 失败重试 1 次；`refreshAll` 不再失效账户列表。
3. **重置按钮文案**：明确为「重置投资账本数据（清除 Cookie + 账本同步的全部账户）」，二次确认后执行，之后需重新获取 Cookie 并登录。
4. **金额显示完整数字**：取消万/亿缩写，全部显示完整数字 + 千分位（如 1,290,105.21）。

## 0.1.88-0.19 (2026-09-09)

### 八项修复

1. **汇总卡布局统一**：累计盈亏固定显示在总资产右侧（不再漂移），本金编辑入口保持在累计盈亏处
2. **总资产按累计盈亏正负标色**（盈利红/亏损绿）
3. **刷新机制重做**：去掉 15s 自动轮询（数据落本地后不再反复拉取）；后端定时同步改为**交易日（周一至五）北京时间 9:31 和 16:05 各一次**；手动「同步」随时可用；收益换算基于本地快照+日K 历史
4. **删除多余的通用「刷新」按钮**，保留投资账本「同步」
5. **账户页签排序**：「默认账户」固定排最后，其他账户按创建时间
6. **记住上次选中的账户**（localStorage，跨重启），切换时同步后端
7. **修复累计盈亏方向**：改为 累计盈亏 = 总资产 - 初始本金（本金 2 万/资产 2.1 万 → +1000 ✓）
8. **重置语义修订**：设置里的重置 = 清除投资账本 Cookie 配置 + 删除账本同步生成的全部账户（持仓/快照/资金），二次确认后执行；之后需重新获取 Cookie 并登录

## 0.1.88-0.18 (2026-09-09)

### 持仓页面重构 + 数据一致性修复

- **修复账户数据混乱（根因）**：账户页签此前只改前端状态，后端 active 未跟随 → 资金等无 account 参数的请求写错账户。现页签切换即同步后端，且所有变更请求显式携带账户参数；选中账户被删除/重置时自动切回。
- **修复重置后无法刷新**：重置后账户列表自动失效并自愈，点「投资账本刷新」即可重建全部账户数据。
- **初始本金交互简化**：取消「资金设置」按钮；本金在汇总卡「累计盈亏」处直接点击编辑（每账户独立），累计盈亏 = 初始本金 - 当前总资产（含累计收益率），现金/出金不再手动维护（现金随投资账本同步）。
- **同步落库投资账本全部字段**：当日参考盈亏/率、持有期盈亏/率、持有天数、持仓占比、近1/3/6/12月涨跌。
- **明细表增强**：新增 当日参考盈亏 / 持有天数 / 近1月 / 近3月 / 近6月 / 近12月 / 占比 七列（全部可排序）。

## 0.1.88-0.17 (2026-09-09)

### 投资账本真实同步打通

- **挖到并验证了投资账本 PC 端真实 API**：`/caishen_httpserver/tzzb/caishen_fund/pc/*`（账户列表/资产趋势/持仓快照/港币汇率等 50+ 端点），同步不再依赖猜测端点。
- **Cookie 获取改为专用 Chrome 配置目录 + CDP**：点「打开投资账本登录窗口」用独立配置登录一次（登录态持久化），之后同步通过调试协议自动读取完整 Cookie（含 httpOnly）。放弃了解密浏览器 Cookie 库的方案（新版 Chrome 加密已不可离线解密）。
- **真实同步落地**：遍历投资账本里的券商账户（本机实测 3 个：中信万一/野猪/中银证券 107W0754）→ 每账户持仓（代码自动映射 SH/SZ/HK/BJ）+ 可用资金 → 写入对应本地账户 + 当日快照。
- 修复：对话框「我已登录，立即同步」后失败重开窗的循环；同步失败只标记不再误清 Cookie。

## 0.1.88-0.16 (2026-09-09)

### Cookie 自动读取 + 输错重试修复

- **「自动读取浏览器 Cookie」按钮**：从本机 Chrome（及 Edge/Brave）解密 10jqka 域 Cookie 自动填入——原理为钥匙串 Safe Storage 密钥 + AES 解密，仅读取 10jqka 域、不出本机；**首次会弹 macOS 钥匙串授权框，点「始终允许」即可**。新增 pycryptodome 依赖。
- **Cookie 配置对话框**：替代原来的纯文本输入框——自动读取大按钮 + 手动粘贴区 + 完整操作步骤（Mac 快捷键 ⌥⌘I / Fn+F12、Network 抓 Cookie 步骤、Safari 开发菜单说明）。
- **修复输错卡死**：同步失败自动清除无效 Cookie，再次点击会重新弹出配置对话框，可无限次重试。

## 0.1.88-0.15 (2026-09-09)

### 投资账本定时同步 + 数据时间提示

- **每小时后台自动同步**：服务启动即挂载定时线程（3600s），已配置 Cookie 时自动拉取投资账本数据更新本地持仓；未配置时静默跳过。手动「刷新」仍在点击瞬间执行。
- **数据时间提示**（「投资账本导入」左侧）：同步成功 → 绿点 +「数据 HH:MM」；未获取成功或失败 → 黄点「重新导入投资账本数据」点击即重试。

## 0.1.88-0.14 (2026-09-09)

### 投资账本对接入口 + 页签 + 重置修复

- **投资账本导入/刷新按钮**（页头）：点击从同花顺投资账本 (tzzb.10jqka.com.cn) 拉取数据，成功/失败都有 toast 提示；首次点击引导粘贴浏览器 Cookie（本地保存）。账本无公开 API 且需登录——接口探测内置候选列表，命中即缓存；未命中会明确提示（后续配合浏览器抓包更新端点即可）。
- **账户改为页签**：每个页签显示账户名 + 当日总盈亏（金额+百分比，红绿标色），「+」号新建账户。
- **修复重置失效**：POST /reset 缺请求体被 422 拒绝；同时修复资金设置接口 500（缺少 request 参数）。

## 0.1.88-0.13 (2026-09-09)

### 多账户 + 设置中心 + 人民币口径

- **多账户**：持仓/快照/资金按账户隔离（旧数据自动迁移到「默认账户」）；页头账户下拉切换、新建/删除账户；所有汇总/收益按账户独立。
- **持仓设置**（页头右上角齿轮）：① 重置数据（清空当前账户持仓+快照+资金，二次确认）② 港币/美元兑人民币汇率（外币市值折算进总资产与收益，修正此前外币直入人民币口径的问题）③ 港股通押金率（默认 3%，成本口径说明：参考汇率+押金预冻结、日终清算多退少补）④ 收益基准切换（上证/深成/恒生/标普）⑤ 快照保留份数（超出自动清理）。
- **累计盈亏**：汇总卡总资产右侧新增「累计盈亏 = 本金 - 出金 - 总资产」（红绿标色 + 累计收益率）；资金设置增加「累计出金」持久化。
- 兼容：`/api/holdings/*` 全部支持 account 参数，缺省为 active 账户。

## 0.1.88-0.12 (2026-09-09)

### 修复截图导入卡死 + 核准改为累计盈亏

- **修复汇总确认 0 条卡住**：逐张确认进入汇总时读到未更新的状态（React setState 竞态），改为局部变量传递，逐张/汇总全流程畅通。
- **核准改「累计盈亏」**：去掉每次的差额告警；本金**默认自动读取持仓页资金设置**，新增出金输入（默认 0）；累计盈亏 = 本金 - 出金 - 总资产，红绿标色。截图总盈亏照旧展示，两者并存互不干扰。

## 0.1.88-0.11 (2026-09-09)

### 持仓截图识别扩展 + 整页拖拽

- **整页拖拽识别**：持仓页任意位置拖入截图即可自动选图并开始识别（带拖入高亮提示），无需先进导入弹窗。
- **识别汇总区**：AI 识别现同时提取截图汇总区全部信息——总资产、总盈亏、当日参考盈亏（额+%'）、总市值、可用、可取、仓位，确认/汇总面板展示。
- **总盈亏核准**：输入初始本金 → 自动计算「本金 - 总资产」与截图总盈亏比对，一致显示 ✓，有差额提示 ⚠（本金口径或期间出入金）；导入时本金可写回资金设置。
- **现金同步**：今日导入时可勾选将截图「可用」同步为现金。
- **修复**：确认面板成本输入框被截断（无效 Tailwind 类 w-18 → w-20）。

## 0.1.88-0.10 (2026-09-09)

### 持仓导入界面改版

- **最近截图网格**：一行 5 个大预览（高度 112px），竖向滚动，选中的高亮描边。
- **逐张确认 + 左侧预览**：识别后弹窗改为左右分栏（宽 62rem）——左侧整幅显示正在核对的截图（等高居中），右侧为该张的识别结果行；「确认本张，下一张」逐张过完 → **汇总确认**（多图去重合并，同持仓取数量大者），再选日期/现金导入。
- **当日持仓语义**：持仓明细仅显示当日持仓；无当日数据时显示「导入当日持仓截图以便于进行核查同步」引导（附一键导入按钮 + 手动添加）。

## 0.1.88-0.09 (2026-09-09)

- **视觉模型缓存失效**：保存/清空 AI 设置后自动重置视觉模型探测缓存，切换模型立即生效（此前需重启服务）。
- 自选导入弹窗：最近截图条已选高亮修复（此前勾选态不正确）。
- 已验证：用户切换 deepseek-v4-flash-vision-exp 后，截图识别 + stockocr 本地验证 6/6 全部通过。

## 0.1.88-0.08 (2026-09-09)

### 识别引擎大修 + stockocr 本地扫描验证

- **修复识别空白**：用户配置的 deepseek 模型为纯文本模型，图片请求被静默忽略。AI 视觉通道现自动探测视觉模型（设置项 `ai_vision_model` → 当前模型名含 vision → 模型列表中第一个 vision 模型），ocr-status 返回解析到的模型与原因。
- **识别改 JSON 结构化输出**：`[{market, code, name, qty, available, cost}]`，替代按列位置的行解析；名称反查同市场优先；已清仓 (qty=0) 行保留；成本价剥离货币前缀 (HK$/$/¥) 且要求完整小数位。实测两张真实券商截图 13/13 全字段正确。
- **稳健性**：reasoning 模型偶发耗尽 token 导致内容为空 → 自动重试（最多 2 次）；AI 输入不再反相灰度（保留彩色，长边 ≤2200px）。
- **stockocr (macOCR/Apple Vision) 本地扫描验证**：AI 识别后自动用本机 stockocr (-l zh-Hans) 二次扫描，交叉验证每条候选的名称与数量，前端显示 ✓（通过）/ ⚠（请核对）。本地、免费、无配额；未安装时跳过验证不影响识别。
- **导入弹窗升级**（持仓 + 自选）：**最近截图/照片横条**（下载/桌面/图片/图库已授权时，按时间倒序，点击即选入）；支持**多张图**顺序识别合并（同持仓取数量大者）；确认行**全部可编辑**（数量/可用/成本/勾选/删除/手动补行）；**导入日期按图片日期预填**可改，历史日期存快照。
- `stockocr` 安装：`brew install stockocr`（macOCR）。

## 0.1.88-0.07 (2026-09-09)

### 持仓模块增强

- **截图导入 + 日期选择**：持仓页新增「截图导入」——AI 识别（名称/数量/可用/成本）→ 确认候选 → **年/月/日级联日期选择器**（点年切年网格、点月切月网格，同券商 App 交互）。今日导入直接更新当前持仓；**历史日期存为该日快照**（可同时记录当日现金），收益按快照分段回算。
- **收益引擎分段回算**：`/api/holdings/pnl` 按快照时间线分段（各段各自持仓/现金）；无快照时回退为当前持仓回算整个区间。新增 `GET /api/holdings/pnl/day/{date}` 当日各持仓盈亏明细。
- **AI 组合体检**：`POST /api/holdings/analyze` SSE 流式报告（组合概览/仓位集中度/盈亏贡献/风险暴露/关注方向），复用 AI 设置，流中自动剥离 reasoning 模型的 `<think>` 段。
- **界面强化**：汇总卡字体放大（总资产 3xl+），当日盈亏/总盈亏**金额与百分比均红绿标注**；汇总卡内嵌近 30 日迷你资产曲线；**日/月/年收益区移至页面最下**。
- **持仓明细表**：全部数值列点标题排序（默认市值降序）；**行点击打开个股详情弹窗**；卖出弹窗「实时」按钮一键取最新价。
- **收益日历**：点击某日弹当日各持仓盈亏明细（涨跌幅+金额，红绿标注）。
- **实时性**：持仓列表/汇总 60s → 15s 轮询 + 窗口聚焦即刷。

## 0.1.88-0.06 (2026-09-09)

### 新增「我的持仓」模块

- **持仓记录**（`data/user_data/holdings.parquet` + `portfolio.json`）：每只持仓的数量/可用/成本，卖出记录已实现盈亏后关闭标记；初始资金与现金手动维护。
- **独立页面 `/holdings`**（导航「持仓」）：
  - 顶部汇总卡：总资产（现金+市值）、当日盈亏、总盈亏（浮动+已实现）、仓位%、持仓数
  - **日/月/年收益**：日=收益日历热力图（红盈绿亏）、月=12 个月柱状图、年=资产曲线+上证指数基准对比
  - 持仓明细表：现价/涨跌幅/持仓可用/成本/市值/浮动盈亏/当日盈亏 + 编辑/卖出（预计盈亏预览）/删除
  - 持仓结构饼图 + 个股盈亏贡献排行
  - 手动添加持仓、资金设置弹窗
- **收益口径**：快照+日K回算（A股本地批量、港美股单只拉取），当日叠加实时行情；卖出实时盈亏计入「已实现」。
- **截图导入到持仓**：自选导入弹窗新增「导入到持仓」按钮，连同识别的数量/可用/成本写入并自动加自选；AI 识别 prompt 增加「可用数量」列。
- **持仓标记**：自选列表（表格+卡片）与个股详情页显示「持 xx」紫色徽章（数量+成本悬浮提示）；卖出后徽章自动消失。
- **持仓异动监控**：监控规则作用范围新增「我的持仓」选项，规则随持仓变化自动生效，复用现有告警推送通道。
- API：`GET/PUT/DELETE /api/holdings/*`、`/summary`、`/pnl`、`/benchmark`、`/import`、`/{symbol}/sell`、`PUT /portfolio`。

## 0.1.88-0.05 (2026-09-08)

### 修复：个股分析港美股获取不到日K

- **根因**：`/api/stock-analysis/levels` 与 AI 个股分析（`stock_analyzer._load_kline`）只读 A股 enriched 缓存，港美股为空时直接返回「无数据」，没有实时拉取兜底。
- **修复**：新增 `kline_sync.fetch_hk_us_daily_with_indicators()`（单只 klines.get 拉日K + compute_enriched 现算指标，连板自然归零）；上述两处在缓存为空且标的为 .HK/.US 时自动兜底。
- 实测：智谱 02513.HK（120 根日K、压力支撑/枢轴点全套价位）、苹果 AAPL.US 均正常；A股回归不变。

## 0.1.88-0.04 (2026-09-08)

### 修复：自选列表港美股不显示名称与行情

- **根因**：`/api/watchlist/enriched` 只对 A股 enriched 缓存做 LEFT JOIN，港美股全部字段为 null，列表渲染为整行「—」。
- **修复**：港美股自选行的现价/涨跌幅/涨跌额/成交额/振幅/换手率改由 **TickFlow 实时行情**填充（quotes 接口完整支持 .HK/.US）；名称沿用港美股字典解析。
- 响应新增 `region` 字段（CN/HK/US）；自选列表代码列为港股/美股显示「港」「US」标签徽章。
- 技术指标列（量比/RSI/动量/连板等）对港美股仍为「—」（依赖 A股 enriched 计算，暂不适用）。

## 0.1.88-0.03 (2026-09-08)

### 版本号标注分支版本 + 控制台图标更换

- **版本号显示统一为分支版本**（上游版本-修订序号）：`/health` 与 `/api/data/version` 的 `version` 字段返回 `0.1.88-0.03`，新增 `upstream_version` 字段保留上游原始版本；FastAPI 应用元数据与启动日志同步。
- **设置 → 系统**：「版本」显示分支版本并注明上游版本；「检查更新」链接指向本分支仓库 Releases（cafeteaorme/tick-stock-panel）。
- **「TickFlow 控制台」App 图标更换**为用户提供的图片（1080×1080 → 多尺寸 icns，重签名）。
- 后端新增 `app.__revision__` / `app.branch_version()`：每次发布递增 `__revision__` 即可，所有展示位自动生效（含控制台 App 状态卡与顶栏的 v 版本号）。

## 0.1.88-0.02 (2026-09-08)

### 港股 / 美股分时图（腾讯免费接口 + 设置开关 + 点击加载 + 当日自动刷新）

- **数据源**：TickFlow 分钟权限仅覆盖 A股（实测 00700.HK / AAPL.US 均报无权限），改用**腾讯免费行情接口**（`web.ifzq.gtimg.cn`，https、无需 key）拉取港美股当日分时；美股自动探测交易所后缀（.OQ/.N/.A）并缓存。含 SSRF 防护（域名白名单 + 解析 IP 校验 + 禁重定向）。
- **新增 `backend/app/services/hk_us_intraday.py`**：分时拉取、解析（累计量转每分钟增量）、落盘（`data/kline_minute_hk_us/{YYYYMMDD}.parquet`，每日一文件）、「当日已看清单」（`data/user_data/hk_us_intraday_viewed.json`，跨交易日自动重置）、后台刷新线程（60s 一轮，仅刷新当日已看且对应市场开市中的标的，收盘后停止）。
- **设置开关 `hk_us_intraday_enabled`（默认关）**：`PUT /api/settings/preferences/hk-us-intraday`；关闭时 `/api/kline/minute` 对 `.HK/.US` 返回 403、后台刷新零请求——功能整体休眠。前端「数据」页分钟K配置区新增「港美股分时图」开关。
- **数据量控制**：开关默认关；开启后个股详情页才出现「分时」入口，**点击才拉数据**；仅当日已看标的进入后台刷新；不做历史批量同步。
- **`/api/kline/minute` 区域分流**：港美股走腾讯接口（实时拉取→落盘→标记已看，失败回退本地），A股路径完全不变；`minute-batch` 剔除港美股避免 TickFlow 分钟权限报错。
- **前端 `EChartsIntraday` 时间轴区域化**：A股 242 点 / 港股 331 点（9:30–12:00 + 13:00–16:00）/ 美股 391 点（美东 9:30–16:00）；分时均线成交量单位 A股按手、港美股按股；涨跌停线与 limit 模式仅 A股。
- **`StockIntradayChart`**：港美股空数据时提示「当日暂无分时数据（休市/未开盘）」+ 重新获取（不再弹 A股的 5 日分钟K同步窗）；有数据时 60s 轮询。

### 已验证

- 开关关 → 403；开启后 `00700.HK` 实时分时 147 根、落盘、二次打开走本地
- 后台自动刷新实测：港股开市中已看标的 60s 后 147→149 根
- A股分时回归正常（平安银行 121 根）
- 美股劳动节休市优雅提示（source=none）

## 0.1.88-0.01 (2026-09-06)

### 港股 / 美股支持（基础版：自选 + 搜索 + 日K）

- **新增 `backend/app/markets.py`**：按代码后缀（`.SH/.SZ/.BJ` → CN、`.HK` → HK、`.US` → US）统一识别市场区域，作为所有 A股特有逻辑的分流入口。
- **港美股标的字典**：新增 `backend/app/services/hk_us_instruments.py`，通过 TickFlow SDK 拉取港股全量（2845 只）与美股全量（12307 只）清单，存入 `data/instruments_hk/`、`data/instruments_us/` 独立目录 —— **不并入 A股 instruments**，选股器 / 回测 universe 不受影响。该字典同时作为截图识别的港美股代码匹配词典。
  - 新增 API：`POST /api/kline/sync_instruments_hk_us` 手动触发同步；首次搜索港美股时自动后台同步。
- **涨跌停规则**：`price_limits.py` 对 HK/US 返回「无涨跌停」哨兵值，连板计算自然归零，A股规则不变。
- **自选与搜索**：自选可直接添加任意 `.HK` / `.US` 代码；标的搜索返回港美股结果并带 `region` 字段（前缀匹配优先排序）；自选名称解析合并港美股字典。
- **日K数据**：`klines.batch` 为 A股专属权限，港美股自动改走单只 `klines.get` 拉取（实时拉取 + 本地算指标，与 Free 用户兜底路径一致）。
- **行情落盘隔离**：实时行情轮询不再把港美股蜡烛写入 A股 `kline_daily` 分区。
- **交易时段**：`market_time.py` 新增港股（09:30–16:00 HKT）与美股（美东 09:30–16:00）开闭市判断 `market_open_now(region)`（不含节假日日历）。
- **前端**：自选搜索结果对港股/美股显示区域标签（「港」/「US」）。

### 持仓截图识别增强（A股 + 港股 + 美股）

- **AI 视觉通道（主通道）**：新增 `backend/app/services/watchlist_ocr/ai_vision.py`，复用设置页已配置的 openai 兼容客户端，将截图交由多模态模型识别，输出结构化持仓行（市场 / 代码 / 名称 / 数量 / 成本），自动剥离 `<think>` 推理文本与 markdown 围栏，只保留合法数据行。安全约束：仅允许 https 且拒绝内网/环回地址。
- **Tesseract 通道（兜底）**：需本地安装 `tesseract` + `tesseract-lang`（macOS：`brew install tesseract tesseract-lang`）。AI 未配置或失败时自动回退。
- **解析管线重写** `watchlist_ocr/pipeline.py`：
  - 按行解析，A股 6 位码 → 美股字母码（含 OCR 混淆修正 0↔O/1↔I/5↔S/8↔B/2↔Z，支持 `BRK.B`）→ 港股 3–5 位码（补零变体匹配），互不抢占；
  - **名称反查兜底**：截图不显示代码时按名称匹配字典（去空格归一，同市场优先、最长命中优先）—— 经真实中银证券持仓截图验证，无代码场景下 5/5 全部正确识别（含新股与港股）；
  - **数量 / 成本提取**：遮蔽代码 token 后按规则提取（支持 `1000股`、`1,000`、`2万`、`×100` 等形态）；
  - 候选字段新增 `market` / `qty` / `cost`。
- **OCR 状态**：`GET /api/watchlist/ocr-status` 返回双引擎状态 `{engines: {tesseract, ai_vision}}`。
- **前端导入弹窗**：候选列表显示市场标签（A股/港股/美股）与持仓数量、成本价。

### 其他

- 分支版本：0.1.88-0.01（上游版本 0.1.88 保持不变，后缀序号为本分支修订号）。

### 已验证

- 真实券商持仓截图（中银证券，无代码显示）：AI 通道识别 5/5（长鑫科技 688825、海光信息 688041、诺普信 002215、粤传媒 002181、港股 MINIMAX-W），数量与成本价全部正确。
- `00700.HK` / `AAPL.US` 搜索、加自选、日K（30 根，腾讯 442.8 / 苹果 319.97）端到端通过。
